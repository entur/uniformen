[install]
exact = true
ignoreScripts = true
# Only install package versions published at least 3 days ago
minimumReleaseAge = 259200

[test]
# Sets the Auth0 env vars config.ts requires, before any test module loads.
preload = ["./src/test/authTestSetup.ts"]

